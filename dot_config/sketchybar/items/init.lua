require("items.paw")
-- macOS menu items are disabled because they are noisy and unreliable here.
-- Mission Control spaces are enabled, but yabai actions are intentionally removed.
require("items.spaces")
require("items.calendar")
require("items.widgets")
require("items.media")
